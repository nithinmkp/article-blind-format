$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$for(by-author)$
\author{
  $it.name.literal$%
  $if(by-author.attributes.corresponding)$\thanks{Corresponding author.\\ Email: \texttt{$by-author.email$}}$endif$%  % Add corresponding author footnote if applicable
  $if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$  % Add acknowledgments if applicable
}

\affil{$for(it.affiliations)$ 
  $if(it.department)$$it.department$$if(it.name)$, $endif$$endif$  % Department, followed by a comma if institution is present
  $if(it.name)$$it.name$$endif$  % Institution name
  $sep$  % Ensure proper separation of affiliations
$endfor$}
$endfor$

$if(date)$
\date{$date$}
$else$
\date{}  % Leaves the date blank if no date is provided
$endif$
