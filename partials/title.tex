$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
$endif$

$for(by-author)$
\author{$it.name.literal$ 
$if(it.acknowledgements)$\thanks{$it.acknowledgements$}$endif$} 
\affil{$for(it.affiliations)$ 
$if(it.department)$$it.department$$if(it.name)$, $endif$$endif$ % Department, if available, followed by a comma if institution is present
$if(it.name)$$it.name$$endif$ 
$endfor$}

$sep$
$endfor$

$if(date)$
\date{$date$}
$else$
\date{} % Leaves the date blank if no date is provided
$endif$


